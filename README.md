# mock-server

### how to start?

1. install the dependences
```shell
$ cd mock-server
$ npm install
```

2. run json-server
```shell
$ npm start
```

3. run `index.js` in a new terminal
```shell
$ node index.js
```
